{
  "mcpServers": {
    "supabase": {
      "type": "http",
      "url": "https://mcp.supabase.com/mcp?project_ref=qtqignacbrwhblgjbxat"
    },
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    }
  },
  "permissions": {
    "allow": [
      "Bash(npm run lint)",
      "Bash(npm run build)",
      "Bash(npm run dev)",
      "Bash(npm run format)",
      "Bash(npx tsc --noEmit)",
      "Bash(npx prisma migrate dev*)",
      "Bash(npx prisma db push)",
      "Bash(npx prisma generate)",
      "Bash(npx prisma db seed)",
      "Bash(graphify*)",
      "Edit(.claude/**)",
      "Write(.claude/**)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "node .claude/hooks/block-env.js" }]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('tool_input',d).get('command',''))\" 2>/dev/null || true); case \"$CMD\" in *grep*|*rg\\ *|*ripgrep*|*find\\ *|*fd\\ *|*ack\\ *|*ag\\ *)   [ -f graphify-out/graph.json ] &&   echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: knowledge graph at graphify-out/. For focused questions, run `graphify query \\\"<question>\\\"` (scoped subgraph, usually much smaller than GRAPH_REPORT.md) instead of grepping raw files. Read GRAPH_REPORT.md only for broad architecture context.\"}}' || true ;; esac"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit(src/**/*.ts)|Write(src/**/*.ts)",
        "hooks": [
          { "type": "command", "command": "npx tsc --noEmit" },
          { "type": "command", "command": "npm run lint" },
          { "type": "command", "command": "graphify update . 2>/dev/null || true" }
        ]
      },
      {
        "matcher": "Edit(prisma/schema.prisma)|Write(prisma/schema.prisma)",
        "hooks": [
          { "type": "command", "command": "npx prisma generate" },
          { "type": "command", "command": "graphify update . 2>/dev/null || true" }
        ]
      },
      {
        "matcher": "Edit(src/routes/**)|Write(src/routes/**)",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"process.stdout.write(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'Route file modified. Invoke the api-contract-reviewer agent now to verify the endpoint matches api-specs.md — required before considering the task complete.'}}));\""
          }
        ]
      },
      {
        "matcher": "Edit(src/middleware/**)|Write(src/middleware/**)",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"process.stdout.write(JSON.stringify({hookSpecificOutput:{hookEventName:'PostToolUse',additionalContext:'Middleware modified. Invoke the security-reviewer agent now to check for auth vulnerabilities and ownership issues — required before considering the task complete.'}}));\""
          }
        ]
      }
    ]
  }
}
